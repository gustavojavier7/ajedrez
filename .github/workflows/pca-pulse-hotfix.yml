name: Apply PCA pulse realtime fixes

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/pca-pulse-hotfix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply patch
        run: python tools/pca_pulse_hotfix.py

      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/pca-pulse-hotfix.yml tools/pca_pulse_hotfix.py
          git add index.html
          git commit -m "Complete PCA pulse realtime metrics"
          git push origin HEAD:main
